i have three tasks for you, RULE: FIRST AND LAST RULE IS TO DO NOT CHANGE/MODIFY/UPDATE/EDIT any single file

Move Step by step as directed

read every single file in ./backend.
Then same with ./frontend directory.
EVERY SINGLE FILE MEANS EVERY SINGLE FILE!!!!!
then run these  script files:
 ./backend/scripts/test_all.py 
         &
 ./scripts/verify-e2e.sh

Last: Give me what you acheived 

-------------------------------------------------
/sp.implement
I am proceeding with the "001-frontend-infra" feature. I have a detailed file analysis report of the current broken state of the frontend. 

Your goal is to execute the fixes outlined in the Specification and the Analysis Report below. 

### Instructions:
1. **Refactor Strategy**: 
   - First, resolve the `lib/api-client.ts` vs `lib/api.ts` conflict. Create the definitive `lib/api-client.ts` and delete `lib/api.ts`.
   - Update `app/actions/tasks.ts` and `lib/auth-client.ts` to use the unified client.
2. **Auth Repair**: 
   - Create `app/api/auth/token/route.ts` if it is the cleanest way to bridge the gap, or update the client to access the session token directly.
   - Fix `middleware.ts` cookie targeting.
3. **Cleanup**: 
   - Go through the file list and remove unused `userId` parameters.
   - Fix the `/health` vs `/api/health` mismatch.

### Input Data
Here is the detailed analysis of the current defects. Use this as your checklist for what to fix:
/home/ahsan/Dev/Hackathons/Hackathon-II/evolution-of-todo/FRONTEND CODE ANALYSIS REPORT
### Output
Please generate the code blocks for the modified files. Start with the "Critical" issues defined in the report.


---------------------------------------------


 here is a tasks for you.
| HIGH     | tailwindcss-v4-guide        | Tailwind v4 is a major rewrite (CSS-first, no config file) |
  | HIGH     | framer-motion-guide         | Complex animations used heavily in your app                |
  | HIGH     | zustand-guide               | State management for UI (modals, filters, toasts)          |
  | MEDIUM   | tanstack-query-guide        | Server state management (though you use Server Actions)    |
  | MEDIUM   | next-themes-guide           | Dark mode support                                          |
  | LOW      | canvas-confetti-guide       | Simple lib, just one function                              |
  | LOW      | next-view-transitions-guide | View routing transitions                                   |
    them using skill-forger skill and with the help of context7


----------------------------------------------------------

Do these step by step:
  read: /home/ahsan/Dev/Hackathons/Hackathon-II/evolution-of-todo/Hackathon-docs/Hackathon2_doc.md

  read every single file in: backend/
  Add Backend README.md,an Industry level Readme file with the new context you got from reading these files
  Create CLAUDE.md in it and fill it with the context you got after reading it

  read every single file in: frontend/
  Update Frontend README.md to an Industry level Readme file with the new context you got from reading these files
  Create CLAUDE.md in it and fill it with the context you got after reading it


  Read Every Single File in src/
  Add src's README.md,an Industry level Readme file with the new context you got from reading those files
  Create CLAUDE.md in it and fill it with the context you got after reading it


  Now at the full project level
  Update Project README.md to an Industry level Readme file with the new context you got from reading those files
  Update CLAUDE.md with all the context you got
  


-----------------------------

I need you to perform a documentation overhaul. Please follow these steps sequentially.

**Global Constraints:**
- When reading directories, **strictly ignore** `node_modules`, `dist`, `build`, `coverage`, `.git`, and lock files (package-lock.json/yarn.lock).
- "Industry Level README" means: clearly labeled badges, Tech Stack, Setup/Installation instructions, Environment Variables, API documentation (if applicable), and contribution guidelines.
- `CLAUDE.md` should contain a summary of the architectural patterns, key file locations, and project-specific coding conventions found in that directory.

**Step 1: Context Loading**
Read `/home/ahsan/Dev/Hackathons/Hackathon-II/evolution-of-todo/Hackathon-docs/Hackathon2_doc.md` to understand the project goals.

**Step 2: Backend Documentation**
Read the source code files in `src/` (focusing on entry points, controllers, models, and config, every-file).
- Create/Update `src/README.md` to an Industry Level standard.
- Create `src/CLAUDE.md` with the backend context and conventions.

**Step 3: Backend Documentation**
Read the source code files in `backend/` (focusing on entry points, controllers, models, and config).
- Create/Update `backend/README.md` to an Industry Level standard.
- Create `backend/CLAUDE.md` with the backend context and conventions.

**Step 4: Frontend Documentation**
Read the source code files in `frontend/` (focusing on components, pages, hooks, and utils).
- Create/Update `frontend/README.md` to an Industry Level standard.
- Create `frontend/CLAUDE.md` with the frontend context and conventions.

**Step 5: Project Root Documentation**
Based on the context gathered from the previous steps:
- Update the root `README.md` to be an entry point for the whole repo, linking to the sub-READMEs and explaining the overall architecture.
- Update the root `CLAUDE.md` with high-level architectural rules and workflow preferences.

---------------------


/sp.specify

# Feature: Advanced Dashboard UI Overhaul & Feature Completion

## 1. Context & Objective
The backend (FastAPI) and Frontend logic (API Client, Zustand Store) currently support Advanced features (Recurring Tasks, Due Dates, Tags) and Intermediate features (Sort, Search, Filter). However, the current Dashboard UI (`dashboard-content.tsx`) is functionally incomplete and visually "basic." 

**Objective:** Transform the Dashboard into a high-fidelity, "Glassmorphism" aesthetic that matches the **Hero/Landing Page** design, while implementing the missing UI controls for all Advanced features.

## 2. Design System & Aesthetics (Source of Truth)
**Reference File:** `app/page.tsx` (Hero Page) & `app/layout.tsx`
The agent must analyze the Hero page to extract the design tokens. The Dashboard must use:
* **Theme:** "Deep Space" Glassmorphism.
* **Backgrounds:** Rich gradients, blur effects (`backdrop-blur-md`), and semi-transparent card backgrounds (e.g., `bg-white/10` or `bg-black/20`).
* **Typography:** Match the Hero page font weights, tracking, and gradient text styles.
* **Animations:** Use Framer Motion (or existing animation utils) for staggered list entry and smooth state transitions (like the Hero page elements).

## 3. Functional Requirements

### A. Advanced Task Creation (Update `TaskForm`)
The current form only allows Title, Description, and Priority. It must be updated to include:
1.  **Due Date Picker:** A datetime-local input (styled) to save to `due_date`.
2.  **Tag Management:** A UI to add/remove tags (visual chips with colors) mapped to the `tags` array.
3.  **Recurrence Selector:** A dropdown/select menu for `recurrence_pattern` (Options: NONE, DAILY, WEEKLY, MONTHLY).

### B. Dashboard Toolbar (New Component)
Replace the basic header with a cohesive Toolbar containing:
1.  **Search Bar:** Real-time text input (debounced) connected to `api.searchTasks`.
2.  **Filter Controls:** * **Status Tabs:** (All / Pending / Completed).
    * **Priority Dropdown:** Filter by High/Medium/Low.
3.  **Sort Controls:** * Dropdown for Criteria: (Created Date, Due Date, Priority, Title).
    * Toggle Button: Ascending / Descending.

### C. Task Card Redesign (`TaskCard`)
Update the card component to:
* Match the Glassmorphism aesthetic (remove basic borders, use subtle shadows/glows).
* Display the **Due Date** (highlight red if overdue, orange if due soon).
* Render **Tags** as colored pills/badges.
* Show a **Recurrence Icon** if the task is repeating.

## 4. Technical Constraints
* **State Management:** Must use the existing `stores/ui-store.ts` for managing filter/sort state.
* **API Interactions:** Must use existing methods in `lib/api-client.ts`. Do not write new API fetchers; connect the UI to the existing ones.
* **Responsiveness:** The dashboard must remain fully responsive (Mobile/Desktop).

## 5. Acceptance Criteria
1.  The Dashboard looks visually identical in quality and theme to the Hero page.
2.  A user can create a task with a specific Due Date, Tags, and Weekly Recurrence.
3.  A user can sort the list by "Due Date" (Ascending) to see urgent tasks first.
4.  A user can filter to see only "High Priority" tasks.